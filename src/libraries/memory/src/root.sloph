module memory;

public import core::{Block};
public import core::memory::{AccessError, AllocationError, allocate, capacity, copy, read, release, write};
import sloph::{Unit};

public fn drop(block: Block) -> Unit {
  release(block)
}
