module memory_case::main;

import memory::{AllocationError, Block, allocate, capacity};

fn leaked() -> Result[Int, AllocationError] {
  let block = allocate(64)?;
  Result::Ok[Int, AllocationError](capacity(block))
}

const main: Result[Int, AllocationError] { leaked() }
